# Discovery stages

- `api_endpoint_confirm.py`: bounded, read-only API endpoint confirmation.
- `tool_fingerprint_httpx.py`: controlled HTTP fingerprinting with runtime/tool discovery.

The repository-root files with the same names remain compatibility entrypoints for existing runners, tests, and desktop launchers.

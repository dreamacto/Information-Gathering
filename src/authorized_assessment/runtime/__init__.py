"""Stable package facade for the shared runtime.

The implementation remains at the repository root during the compatibility
migration so existing scripts and desktop launchers keep working.
"""
from exercise_runtime import *  # noqa: F401,F403
